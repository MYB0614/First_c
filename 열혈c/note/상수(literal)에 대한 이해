#include <stdio.h>

int main()
{
    //printf("literal size : %d\n",sizeof(8)); int이니 4? yes
    //printf("literal size : %d\n",sizeof(7.14)); float이니 4? 기본적으로 실수는 double형태여서 8 출력
    //printf("literal size : %d\n",sizeof('A'));  char이니 1? => 'A' = 65이니 int 크기인 4가 출력
    
    //즉 대입연산자 기준으로 동일한 크기를 주려면 접미사를 줘야함
    //예시
    unsigned int num1 = 7U;     //int는 4byte이니 -20억~20억 , unsigned int 는 음수 없으니 0~48억?
    unsigned long num2 = 19UL;  
    unsigned long long num3 = 21ULL;
    
    float f_1 = 3.17F;
    double f_2 = 3.1415;
    long double Fl_2 = 3.192342L;
    
    printf("unsigned int : %u\n",num1);
    printf("unsigned long : %lu\n",num2);
    printf("unsigned long long: %llu\n",num3);
    printf("flaot : %f\n",f_1);
    printf("double : %f\n",f_2);
    printf("long double : %Lf\n",f_1);
    return 0;
    
    /*
    여기서 주의할 점!!
    출력시 
    unsigned int = %u
    unsigned lont = %lu
    unsigned long long = %llu
    
    float int = %f
    double = %f
    long double = %Lf
    
    입력시(scanf)
    => 정수형은 모두 동일함 
    float int = %f
    double = %lf
    long double = %Lf
    
    */
}
